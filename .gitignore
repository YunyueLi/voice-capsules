.DS_Store
HANDOVER.md
_research-archive/
.venv/

# 声部工作区:清洗后语料 + 冻结考卷正文，都是本地工作物，不入公开仓。
# 只提交 语料索引/试点/ 下的「底本说明 + 考卷清单(仅文件名与哈希)」这类元数据。
声部/

# 原文语料：全部本地留，不进公开仓（多为公版，但仓库托管在 GitHub/美国、逐作品法域复杂，
# 统一不在此再分发正文）。公开仓只放已双法域清算的元数据（如 语料索引/试点/ 的底本说明），
# 不列版权期作者的字数（字数=占有全文的自证）。
原文/
